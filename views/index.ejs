<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <ul>
      <% messages.forEach((message)=> { %>
      <li>
        <h2><a href="/show/<%= message.user %>"><%= message.text %></a></h2>
        <p><%= message.user %></p>
        <p><%= message.added %></p>
      </li>
      <hr />
      <% }) %>
    </ul>
    <a href="/new">Add new message</a>
  </body>
</html>
